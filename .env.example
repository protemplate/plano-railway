# ==============================================================================
# Plano Railway Template - Environment Variables
# ==============================================================================

# --- LLM Provider API Keys (at least one required) ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=
# GROQ_API_KEY=
# MISTRAL_API_KEY=
# DEEPSEEK_API_KEY=
# XAI_API_KEY=
# TOGETHER_API_KEY=

# --- Model Selection (optional, defaults shown) ---
# Override the default model for each provider:
# PLANO_OPENAI_MODEL=openai/gpt-4o
# PLANO_ANTHROPIC_MODEL=anthropic/claude-sonnet-4-5
# PLANO_GOOGLE_MODEL=gemini/gemini-2.5-flash
# PLANO_GROQ_MODEL=groq/llama-3.3-70b-versatile
# PLANO_MISTRAL_MODEL=mistral/mistral-large-latest
# PLANO_DEEPSEEK_MODEL=deepseek/deepseek-chat
# PLANO_XAI_MODEL=xai/grok-3
# PLANO_TOGETHER_MODEL=together_ai/meta-llama/Llama-3.3-70B-Instruct-Turbo

# --- Default Provider (optional) ---
# Which provider to use as the default when no routing is configured.
# If not set, the first provider with an API key is used.
# Values: openai, anthropic, google, groq, mistral, deepseek, xai, together
# PLANO_DEFAULT_PROVIDER=openai

# --- Intelligent Routing (optional) ---
# Enable automatic model selection based on prompt intent.
# Requires routing_preferences in custom config (PLANO_CONFIG_YAML).
# PLANO_ROUTING_MODEL=Arch-Router
# PLANO_ROUTING_PROVIDER=arch-router

# --- Request Timeout (optional) ---
# PLANO_TIMEOUT=30s

# --- Observability (optional) ---
# Trace sampling percentage (0-100). 0 disables tracing.
# PLANO_TRACE_SAMPLING=0
# LOG_LEVEL=info

# --- Config Override (optional, advanced) ---
# Provide a complete Plano config to bypass auto-generation.
# Use ONE of these (not both):
# PLANO_CONFIG_YAML=<raw yaml string>
# PLANO_CONFIG_BASE64=<base64 encoded yaml>
